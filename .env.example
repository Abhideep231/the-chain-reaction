# Base URL of the FastAPI backend (Sprints 9-15), no trailing slash.
# Falls back to http://localhost:8000 in local dev if unset — see
# src/lib/api/config.ts.
NEXT_PUBLIC_API_URL=http://localhost:8000
